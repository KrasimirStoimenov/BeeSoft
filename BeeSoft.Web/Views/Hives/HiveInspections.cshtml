@model HiveResourcesViewModel<InspectionListingServiceModel>
@inject IViewLocalizer Localizer

@{
    ViewBag.Title = @Localizer["InspectionsForHive"];
    var inspectionServiceModel = Model.Resources.FirstOrDefault() as InspectionListingServiceModel;
    var hiveId = inspectionServiceModel != null ? inspectionServiceModel.HiveId : 0;
}

<br />
<br />
<div class="container p-3 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">@ViewBag.Title: №@Model.HiveNumber</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Inspections" asp-action="CreateInspection" asp-route-hiveId="@hiveId" class="btn btn-outline-primary"><i class="bi bi-plus-lg"></i> @Localizer["CreateNewHive"]</a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr style="text-align:center">
                <th>
                    @Localizer["InspectionDate"]
                </th>
                <th>
                    @Localizer["WeatherConditions"]
                </th>
                <th>
                    @Localizer["Observations"]
                </th>
                <th>
                    @Localizer["ActionsTaken"]
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var inspection in Model.Resources)
            {
                <tr style="text-align:center">
                    <td>
                        @inspection.InspectionDate
                    </td>
                    <td>
                        @inspection.WeatherConditions
                    </td>
                    <td>
                        @inspection.Observations
                    </td>
                    <td>
                        @inspection.ActionsTaken
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div>
        <partial name="~/Views/Shared/ButtonsPartials/_BackButtonPartial.cshtml" />
    </div>
</div>